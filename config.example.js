/*
 * Template for config.js. Copy this file to `config.js` and fill in the two
 * values. config.js is gitignored; config.example.js is the committed shape.
 */
window.SUPABASE_URL = 'https://<your-project-ref>.supabase.co';
window.SUPABASE_ANON_KEY = '<your-anon-public-key>';
